% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_sets.R
\name{rebuild_manifest}
\alias{rebuild_manifest}
\title{Rebuild manifest for an existing output directory (no re-simulation)}
\usage{
rebuild_manifest(
  base_recipe,
  vary,
  out_dir,
  filename_template = "sc{scenario_id}_r{rep}"
)
}
\arguments{
\item{base_recipe}{A validated recipe list (use \code{validate_recipe()} if needed).}

\item{vary}{Named list used originally in \code{generate_recipe_sets()} for the grid.}

\item{out_dir}{Directory that already contains the datasets.}

\item{filename_template}{The same template you used when writing files
(default \code{"sc{scenario_id}_r{rep}"}). It may also include tokens for dotted paths from \code{vary}.}
}
\value{
The rebuilt manifest (also writes \code{manifest.rds} in \code{out_dir}).
}
\description{
Reads datasets already written in \code{out_dir} and reconstructs a
\code{manifest.rds} with rich metadata (model, baseline, effects, etc.).
}
